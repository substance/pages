const debug = require('debug')('pages:transpile')
// const { parseHTML, walk, CodeGenerator } = require('../build/substance-pages.cjs.js')

module.exports = function transpile(source, ext) {
  // if (ext === '.js') {
  //   return source
  // }
  // if (ext !== '.html') {
  //   throw new Error('Only html is supported')
  // }
  // debug('transpiling HTML file')
  // debug('.. parsing HTML')
  // let ast = parseHTML(source)
  // debug('.... done')
  // let generator = new CodeGenerator()
  // debug('.. transforming AST')
  // walk(ast, generator)
  // debug('.... done')
  // debug('.. generating code')
  // let result = generator.generate()
  // debug('.... done')
  // return result
}